# Версии зафиксированы точно (==), а не "от" (>=) — специально.
# Бот работает без присмотра по расписанию, и на каждом запуске GitHub
# Actions заново устанавливает зависимости "с нуля". Если оставить
# диапазон версий открытым, любой новый релиз пакета на PyPI (в том числе
# потенциально скомпрометированный — такое реально случалось, например
# именно с deep-translator в 2022 году через взлом аккаунта автора)
# автоматически установится в следующем же запуске без вашего ведома.
# Точная версия исключает такой сценарий. Актуальность версий проверена
# на PyPI на момент подготовки этого файла.
requests==2.34.2
feedparser==6.0.12
deep-translator==1.11.4
epicstore_api==0.1.7
